{
  "name": "h5-platform-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "独立 Agent 执行沙箱服务",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/service.js",
    "start": "npm run build && node dist/service.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
